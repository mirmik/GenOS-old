#ifndef GENOSMATH
#define GENOSMATH






template <typename T, typename U, typename V> void limitation(T& a,T min,T max){
		if (a<min) a=min;
		if (a>max) a=max;
		return a;
	};

































#endif

